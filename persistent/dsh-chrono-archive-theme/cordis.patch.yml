# dsh-chrono-archive-theme bundle patch
# Official install channel (dsh plugin --profile web add <this-package>)
# mounts this host half once; client half auto-discovers from node_modules.
- insert:
    - id: chrono-archive
      name: 'dsh-chrono-archive-theme'
